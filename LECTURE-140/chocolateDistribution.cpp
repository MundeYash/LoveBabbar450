

// Chocolate Distribution Problem 

// Concept :sorting and sliding window + max, min element find 

#include <bits/stdc++.h>
using namespace std;


// Important function for placing the meeting
vector<int> maxMeeting(vector<int> s, vector<int> e, int n)
{
    // time : O(n log n ) + O(n), space : O(n)
    vector<int> ans;
    

    return ans;
}

// function for printing answer
void print(vector<int> &a)
{
    int n = a.size();
    for (int i = 0; i < n; i++)
    {
        cout << a[i] << " ";
    }
    cout << endl;
}
int main()
{
    int n = 6;
    

    return 0;
}

/*
gfg : https://practice.geeksforgeeks.org/problems/chocolate-distribution-problem3825/1 (rem )

*/